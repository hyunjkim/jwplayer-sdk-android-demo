package com.jwplayer.opensourcedemo.listeners;

import com.google.android.gms.cast.framework.CastSession;

public interface CustomSessionListener {

    void castSessionInformation(CastSession session);
}
