<com.jwplayer.opensourcedemo.VideoPlayerContainerView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/videoPlayerContainerView"
    android:background="@color/ccl_blue"
    android:layout_width="200dp"
    android:layout_height="0dp">
    <FrameLayout
        android:id="@+id/anotherJWPlayerView"
        android:layout_width="200dp"
        android:layout_height="0dp">
    </FrameLayout>
</com.jwplayer.opensourcedemo.VideoPlayerContainerView>